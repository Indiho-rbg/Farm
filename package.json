const { Telegraf } = require('telegraf');
const bot = new Telegraf('<YOUR_BOT_TOKEN>');

// Обробка команди /start
bot.start((ctx) => {
  ctx.reply('Привіт! Як я можу допомогти?');
});

// Запуск бота
bot.launch();
